# 계정 세트 기능 - 사용 가이드

## ✨ 새로운 기능

이제 **쿠팡 + 네이버 계정을 하나의 세트로 저장**할 수 있습니다!

### 🎯 주요 기능

1. **통합 저장**: 쿠팡과 네이버 계정을 하나의 세트로 저장
2. **원클릭 불러오기**: 세트를 선택하면 모든 정보가 자동 입력됨
3. **여러 세트 관리**: 상황별로 다른 계정 세트 사용 가능
4. **영구 보관**: 데이터베이스에 암호화되어 저장 (절대 날라가지 않음)
5. **자동 동기화**: 서버와 자동으로 동기화되어 어느 컴퓨터에서든 접속 가능

---

## 📋 사용 방법

### 1단계: 계정 세트 저장

1. **반품 관리** 페이지 → **계정 설정** 버튼 클릭

2. **세트 이름 입력**:
   ```
   예: "기본 계정", "회사 계정", "개인 계정"
   ```

3. **쿠팡 계정 정보 입력**:
   - Access Key: `A00492891`
   - Secret Key: `534fcf1c...`
   - Vendor ID: `A00492891`
   - Wing 아이디: `lhs0609` (선택)
   - Wing 비밀번호: (선택)

4. **네이버 계정 정보 입력**:
   - 아이디: `your_naver_id`
   - 비밀번호: `your_password`

5. **"저장하고 계속"** 클릭

→ 쿠팡 + 네이버 계정이 하나의 세트로 저장됩니다!

### 2단계: 저장된 세트 불러오기

1. **반품 관리** → **계정 설정** 버튼 클릭

2. 모달 상단에 **"저장된 계정 세트"** 드롭다운이 표시됩니다

3. **세트 선택**:
   ```
   "기본 계정 ⭐"  ← 기본 세트
   "회사 계정"
   "개인 계정"
   ```

4. 선택하면 **쿠팡 + 네이버 정보가 모두 자동으로 입력**됩니다!

5. 필요하면 수정 후 "저장하고 계속" 클릭

---

## 🔧 백엔드 API 엔드포인트

### 계정 세트 생성
```http
POST /api/account-sets
Content-Type: application/json

{
  "name": "기본 계정",
  "description": "일상 업무용 계정",
  "coupang_vendor_id": "A00492891",
  "coupang_access_key": "...",
  "coupang_secret_key": "...",
  "naver_username": "your_id",
  "naver_password": "your_password",
  "is_default": true
}
```

### 모든 세트 조회
```http
GET /api/account-sets
```

응답:
```json
{
  "success": true,
  "count": 2,
  "data": [
    {
      "id": 1,
      "name": "기본 계정",
      "is_default": true,
      "coupang_account": {
        "vendor_id": "A00492891",
        "access_key": "...",
        "secret_key": "..."
      },
      "naver_account": {
        "naver_username": "your_id",
        "naver_password": "..."
      }
    }
  ]
}
```

### 기본 세트 조회
```http
GET /api/account-sets/default
```

### 특정 세트 조회
```http
GET /api/account-sets/{set_id}
```

### 기본 세트로 설정
```http
POST /api/account-sets/{set_id}/set-default
```

### 세트 삭제
```http
DELETE /api/account-sets/{set_id}
```

---

## 💾 데이터베이스 구조

### account_sets 테이블
```sql
CREATE TABLE account_sets (
    id INTEGER PRIMARY KEY,
    name VARCHAR(200) NOT NULL,
    description VARCHAR(500),
    coupang_account_id INTEGER REFERENCES coupang_accounts(id),
    naver_account_id INTEGER REFERENCES naver_accounts(id),
    is_default BOOLEAN DEFAULT FALSE,
    is_active BOOLEAN DEFAULT TRUE,
    last_used_at DATETIME,
    created_at DATETIME,
    updated_at DATETIME
);
```

### 관계
- `account_sets.coupang_account_id` → `coupang_accounts.id`
- `account_sets.naver_account_id` → `naver_accounts.id`

---

## 🔒 보안

1. **암호화 저장**: 모든 비밀번호와 키는 Fernet 암호화
2. **ENCRYPTION_KEY 고정**: `.env` 파일에 저장 (절대 변경 금지)
3. **데이터베이스 보관**: SQLite 파일에 영구 저장
4. **자동 백업**: `backup_database.py` 실행으로 백업 가능

---

## ✅ 장점

### 이전 방식 (쿠팡/네이버 따로)
❌ 쿠팡 계정 선택 → 네이버 계정 따로 선택
❌ 두 번 클릭 필요
❌ 조합 관리 어려움

### 새로운 방식 (세트 통합)
✅ 한 번에 쿠팡 + 네이버 선택
✅ 원클릭으로 모든 정보 입력
✅ 상황별 세트 관리 간편
✅ 기본 세트 자동 적용

---

## 📝 사용 예시

### 예시 1: 기본 계정 세트
```javascript
이름: "기본 계정"
쿠팡: A00492891 계정
네이버: lhs0609 계정
→ 평소에 사용
```

### 예시 2: 회사 계정 세트
```javascript
이름: "회사 계정"
쿠팡: B00123456 계정
네이버: company@example.com 계정
→ 회사 업무용
```

### 예시 3: 테스트 계정 세트
```javascript
이름: "테스트 계정"
쿠팡: TEST12345 계정
네이버: test@example.com 계정
→ 테스트 및 개발용
```

---

## 🚀 다음 단계

1. **데이터베이스 마이그레이션 실행**:
   ```bash
   venv\Scripts\python.exe -c "from backend.app.database import engine, Base; from backend.app.models import AccountSet; Base.metadata.create_all(engine)"
   ```

2. **백엔드 서버 재시작**:
   ```bash
   start_servers.bat
   ```

3. **프론트엔드에서 테스트**:
   - http://localhost:3000 접속
   - 반품 관리 → 계정 설정
   - 세트 저장 및 불러오기 테스트

---

## 🎉 완료!

이제 계정 관리가 훨씬 편리해졌습니다!
- 한 번 저장하면 영구적으로 보관
- 어느 컴퓨터에서든 불러오기 가능
- 여러 계정 세트를 쉽게 관리
- 쿠팡 + 네이버를 통째로 저장/불러오기

문제가 있으면 백엔드 로그를 확인하세요:
```bash
http://localhost:8000/docs  # API 문서
```
